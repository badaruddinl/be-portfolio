import { schemaBuilder } from '@/utils/schemaBuilder.util'

export const rawGetSocialTypesSchema = schemaBuilder({
  moduleFunction: 'Get Social Types',
  moduleDescription: 'list all social types',
})
