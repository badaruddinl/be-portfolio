import { schemaBuilder } from '@/utils/schemaBuilder.util'

export const rawGetTechStackSchema = schemaBuilder({
  moduleFunction: 'Get TechStacks',
  moduleDescription: 'get all tech stacks with versions',
})
